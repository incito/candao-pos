<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                    xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Styles/Brushes.xaml"/>
        <ResourceDictionary Source="Styles/Button.xaml"/>
        <ResourceDictionary Source="Styles/Converter.xaml"/>
        <ResourceDictionary Source="Styles/CustomerDataTemplate.xaml"/>
        <ResourceDictionary Source="Styles/Images.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    
    <Style TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>
    
    <!--按钮样式-->
    <Style TargetType="dxe:ButtonEdit">
        <Setter Property="AllowDefaultButton" Value="False"/>
        <Setter Property="ShowText" Value="False"/>
        <Setter Property="FontSize" Value="25"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!--按钮样式-->
    <Style x:Key="NumberBtnStyle" TargetType="dxe:ButtonEdit">
        <Setter Property="AllowDefaultButton" Value="False"/>
        <Setter Property="ShowText" Value="False"/>
        <Setter Property="Margin" Value="3"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    
    <Style x:Key="LetterBtnStyle" TargetType="dxe:ButtonEdit">
        <Setter Property="AllowDefaultButton" Value="False"/>
        <Setter Property="ShowText" Value="False"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
    </Style>
    
    <Style x:Key="LetterBtnBigStyle" TargetType="dxe:ButtonEdit" BasedOn="{StaticResource LetterBtnStyle}">
        <Setter Property="FontSize" Value="25"/>
        <Setter Property="Width" Value="35"/>
        <Setter Property="Height" Value="35"/>
    </Style>
    
    <Style TargetType="RepeatButton">
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Focusable" Value="False"/>
        <Setter Property="FontSize" Value="25"/>
    </Style>

    <Style x:Key="RbBtnStyle" TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="10,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <BulletDecorator Background="Transparent">
                        <Border x:Name="bd"  HorizontalAlignment="Center" Background="White" CornerRadius="5">
                            <Border.OpacityMask>
                                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                    <GradientStop Color="Black" Offset="0"/>
                                    <GradientStop Color="White" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.OpacityMask>
                            <ContentPresenter Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </BulletDecorator>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="bd" Property="Background" Value="gray" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="false">
                            <Setter TargetName="bd" Property="Background" Value="{x:Null}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="bd" Property="Background" Value="#FF606060" />
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="bd" Property="Background" Value="LawnGreen" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ToggleButton" x:Key="DietTogBtnStyle">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Bd" Background="White" BorderBrush="Gray" BorderThickness="1.5">
                        <ContentPresenter Content="{TemplateBinding Content}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="Gray"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--TableView列样式-->
    <Style TargetType="{x:Type dxg:GridColumn}">
        <Setter Property="FixedWidth" Value="False"/>
        <Setter Property="AllowEditing" Value="False"/>
        <Setter Property="AllowResizing" Value="True"/>
        <Setter Property="FilterPopupMode" Value="CheckedList"/>
        <Setter Property="HorizontalHeaderContentAlignment" Value="Center"/>
    </Style>

    <Style TargetType="{x:Type dxg:TableView}" x:Key="TableViewBaseStyle">
        <Setter Property="AllowBestFit" Value="True"/>
        <Setter Property="AllowColumnFiltering" Value="False"/>
        <Setter Property="AllowDrop" Value="False"/>
        <Setter Property="ShowGroupPanel" Value="False"/>
        <Setter Property="AllowResizing" Value="False"/>
        <Setter Property="AllowCascadeUpdate" Value="True"/>
        <Setter Property="AllowPerPixelScrolling" Value="True"/>
        <Setter Property="AutoWidth" Value="False"/>
        <Setter Property="RowMinHeight" Value="28"/>
        <Setter Property="VerticalScrollbarVisibility" Value="Auto"/>
        <Setter Property="IndicatorWidth" Value="20"/>
        <Setter Property="RowIndicatorContentTemplate" Value="{StaticResource RowIndexDataTemplate}"/>
    </Style>

    <Style TargetType="{x:Type dxg:TableView}" x:Key="TableViewNavRowStyle" BasedOn="{StaticResource TableViewBaseStyle}">
        <Setter Property="NavigationStyle" Value="Row"/>
    </Style>

    <Style TargetType="{x:Type dxg:TableView}" x:Key="TableViewNavCellStyle" BasedOn="{StaticResource TableViewBaseStyle}">
        <Setter Property="NavigationStyle" Value="Cell"/>
    </Style>

    <Style TargetType="{x:Type dxg:TableView}" x:Key="TableViewNavNoneStyle" BasedOn="{StaticResource TableViewBaseStyle}">
        <Setter Property="NavigationStyle" Value="None"/>
    </Style>
</ResourceDictionary>