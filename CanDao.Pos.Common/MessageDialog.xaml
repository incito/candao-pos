<controls:PosMsgWindow x:Class="CanDao.Pos.Common.MessageDialog"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:controls="clr-namespace:CanDao.Pos.Common.Controls"
                        MinHeight="220" MaxHeight="320" Width="400" SizeToContent="Height"
                        WindowStartupLocation="CenterOwner" WindowCornerRadius="5"
                       Loaded="MessageDialog_OnLoaded">
    <controls:PosMsgWindow.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CanDao.Pos.Common;component/BaseDictionary.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </controls:PosMsgWindow.Resources>
    <DockPanel>
        <Grid DockPanel.Dock="Bottom" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="CancelBtn"
                    Content="取消"
                    IsCancel="True"
                    Width="152" Height="48"
                    HorizontalAlignment="Center"
                    Click="CancelBtn_OnClick"
                    Margin="20,0"/>
            <Button Grid.Column="1"
                    IsDefault="True"
                    Content="确定"
                    HorizontalAlignment="Center"
                    Width="152" Height="48"
                    Click="ButtonConfirm_OnClick"/>
        </Grid>
        <TextBlock MaxWidth="450"
                   Text="{Binding InfoMsg}"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center" VerticalAlignment="Top"
                   FontSize="24" Margin="15,15,0,0"/>
    </DockPanel>
</controls:PosMsgWindow>
