<controls1:PosBaseWindow x:Class="CanDao.Pos.VIPManage.Views.UcVipModifyPswView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls1="clr-namespace:CanDao.Pos.Common.Controls;assembly=CanDao.Pos.Common"
             xmlns:bindConverter="clr-namespace:CanDao.Pos.ResourceService.BindConverter;assembly=CanDao.Pos.ResourceService"
             xmlns:cSystem="clr-namespace:CanDao.Pos.Common.Controls.CSystem;assembly=CanDao.Pos.Common"
             xmlns:operates="clr-namespace:CanDao.Pos.VIPManage.Operates"
             mc:Ignorable="d" 
             Height="510" Width="760" Background="White">
    <controls1:PosBaseWindow.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"></Setter>
        </Style>

        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="{x:Type PasswordBox}">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        </Style>
    </controls1:PosBaseWindow.Resources>
    <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
               <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40*"></ColumnDefinition>
                <ColumnDefinition Width="60*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition Height="50"></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition Height="60"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Text="手机号：" Grid.Column="0" Grid.Row="0"/>
            <TextBlock Text="验证码：" Grid.Column="0"  Grid.Row="1"/>
            <TextBlock Text="新密码：" Grid.Column="0" Grid.Row="2"/>
            <TextBlock Text="确认密码：" Grid.Column="0" Grid.Row="3"></TextBlock>


            <TextBox x:Name="TexTelNum" Grid.Column="1" Grid.Row="0" IsEnabled="False" Text="{Binding Model.TelNum}"></TextBox>
            <TextBox x:Name="TexCode" Grid.Column="1" Grid.Row="1" Text="{Binding Model.Code}" Margin="5,5,75,5" ></TextBox>
            <PasswordBox x:Name="TexPsw" Grid.Column="1" Grid.Row="2" PasswordChar="*" bindConverter:PasswordBoxHelper.Attach="True" bindConverter:PasswordBoxHelper.Password="{Binding Model.Psw,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
            <PasswordBox x:Name="TexPswConfirm" Grid.Column="1" Grid.Row="3" PasswordChar="*" bindConverter:PasswordBoxHelper.Attach="True" bindConverter:PasswordBoxHelper.Password="{Binding Model.PswConfirm,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
            <Button x:Name="BtnReg" Style="{DynamicResource ColBtn}" Margin="16,5,0,5" HorizontalAlignment="Left" Width="133" Height="50" Grid.Column="0" Grid.Row="5" Content="确定" Command="{Binding SureCommand}"/>
            <Button x:Name="BtnCancel" Style="{DynamicResource ColBtn}" Margin="0,0,20,0" HorizontalAlignment="Right" Width="133" Height="50" Grid.Column="1" Grid.Row="5" Content="取消" Command="{Binding CloseCommand}"/>
            <operates:ButtonSend x:Name="BtnSendCode" Grid.Column="1" Grid.Row="1" Style="{DynamicResource ColBtn}" IsStartTime="{Binding IsStartTime}" HorizontalAlignment="Right" Width="55" Height="45" Content="发送" Command="{Binding SendCodeCommand}" Margin="0,0,5,0"/>
        </Grid>
        <cSystem:CtlDigitalKeyboard x:Name="Keyboard" Grid.Column="1"  Height="250" VerticalAlignment="Top" Width="310" />
    </Grid>
</controls1:PosBaseWindow>



